function [X, fval] = optimize(S, V, r, n)
fun = @(X)objective(X, S, n); % objective, make sure sum is correct
nonlcon = @(X)constraints(X, S, V, r, n);
X0 = S;
[X, fval] = fmincon(fun, X0, [], [], [], [], [], [], nonlcon, options);
end